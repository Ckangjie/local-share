{
  "name": "local-share",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "node -e \"const fs = require('fs'); fs.rmSync('dist', { recursive: true, force: true });\" && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build --no-bundle -- -j 2 && node -e \"const fs = require('fs'); fs.copyFileSync('src-tauri/target/release/local-share.exe', './LocalShare.exe'); console.log('✅ 已成功输出至根目录: ' + require('path').resolve('./LocalShare.exe'));\""
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-shell": "^2.0.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "@vitejs/plugin-vue": "^5.1.0",
    "sass": "^1.77.0",
    "vite": "^5.4.0"
  }
}
